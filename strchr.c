#include<conio.h>
#include<conio.h>
#include<string.h>
/*
*/
int main(){
    str[9]="E6 year1";
    char*c=0;
    printf("\n enter a string:");
    gets(str);
    do{
        fflush(stdion);
        printf("\t enter a character:");
        chr=getchar();
        c=strchr(str,chr);
        if(c==Null)
            printf("\t\t threr is no '%c'in the \"%s\".\n",chr,str);
        else
            printf("\t\t'%c'foun at the posi")
    }
